x-service: &base
  image: rover:latest
  network_mode: host
  ipc: host
  entrypoint: /ros_entrypoint.sh
  volumes:
    - .:/ros_ws
  devices:
    - /dev/gpiochip0:/dev/gpiochip0
  privileged: true

services:

  motor_driver:
    <<: *base
    command: python3 /ros_ws/scripts/motor_test.py
