x-service: &base
  image: rover:latest
  network_mode: host
  ipc: host
  entrypoint: /ros_entrypoint.sh
  volumes:
    - .:/ros_ws

services:

  motor_driver:
    <<: *base
    privileged: true
    command: python3 /ros_ws/motor_test.py